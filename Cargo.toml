[package]
name = "devinit"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tera = { version = "1.20" }
clap = { version = "4.6", features = ["derive"] }
dialoguer = { version = "0.12" }
